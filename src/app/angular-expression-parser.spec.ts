import { AngularExpressionParser } from './angular-expression-parser';

describe('AngularExpressionParser', () => {
  it('should create an instance', () => {
    expect(new AngularExpressionParser()).toBeTruthy();
  });
});
